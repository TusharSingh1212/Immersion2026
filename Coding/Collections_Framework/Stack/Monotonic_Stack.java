public class Monotonic_Stack {
    public static void main(String[] args) {
        /*
        Stack<> stack = new Stack<>();
        for i ---> array traverse:
            while(!stack.isEmpty() && condition){
                stack.pop();
            }
            result update

            stack.push(element);
        }*/
    }    
}